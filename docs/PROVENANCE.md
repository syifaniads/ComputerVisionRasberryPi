# Provenance

## Retained 2025 artifact

`Computer_Vision_Riset_Quadcopter_ROBOTIIK.ipynb` is the original repository artifact used as the evidence source for model architecture, dataset, training output, and TensorFlow Lite conversion.

The historical README also contained an all-in-one shell setup and example Raspberry Pi inference script.

## Portfolio extension

The following are later engineering improvements and should not be read as historical execution evidence:

- `src/vision_inference.py`
- `runtime/raspberry_pi_camera.py`
- automated tests and GitHub Actions
- modernized setup script
- model card / benchmarking plan
- architecture visual

This separation keeps the portfolio useful without rewriting the experiment's history.
